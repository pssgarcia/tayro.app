# TAYRO — Contexto do Projeto

> Versionado no repositório — precisa sobreviver entre dispositivos (já foi perdido 2x enquanto gitignored: 2026-06-17 e 2026-08-05; agora versionado de propósito). Detalhe específico de uma máquina só (não do projeto em si) vai em `CLAUDE.local.md`, esse sim gitignored.

## O que é
CRM de creators fitness, **creator-first**. MVP de plataforma de marketing de influência. Infra real = Neon (Postgres serverless).

## Cérebro do produto → `.claude/knowledge/`
Este arquivo é **como construir** (stack, convenções, footguns, o que já está pronto).
O **o quê e por quê** mora em `.claude/knowledge/` — versionado de propósito (ver `.gitignore`):
`vision.md` (os "nunca") · `positioning.md` · `personas.md` · `competitors.md` · `decisions.md` (append-only, com as decisões ABERTAS que bloqueiam trabalho) · `customer-interviews.md` (**n=0** — nenhuma frase do tipo "clientes dizem" tem respaldo hoje) · `roadmap.md`.
Não duplicar conteúdo entre os dois lugares. Fluxo de produto: `/feature` → `/research` → `/architect` → implementar (TDD) → `/review`.

## O que cada capacidade faz agora → `specs/`
Terceira categoria, além das duas acima: `specs/<slug>/spec.md` (raiz do repo, ver `specs/README.md`) descreve o comportamento **vivo** de cada capacidade de domínio (Application, Reward, Instagram sync, etc.) — **editar a spec é o pedido de mudança**, não documentação que se atualiza depois do fato. `/architect` cria e mantém esses arquivos (capacidade nova exige veredito `AGORA` do `/feature`; capacidade existente não — editar a spec é o próprio atalho). `/review` confere deriva: código que mudou comportamento sem atualizar a spec correspondente é achado de revisão. Diferença de `decisions.md`: decisão registra por que decidimos X uma vez e nunca se edita; spec registra o que X faz hoje e se edita sempre que o comportamento muda.

## Stack
- **Monorepo:** Turborepo (npm workspaces) — `apps/api` + `apps/web`
- **API:** NestJS (Clean Architecture) · Prisma · PostgreSQL (Neon) · JWT (access + refresh com rotação SHA-256) · Jest
- **Web:** React + Vite · React Router (BrowserRouter) · Zustand · TanStack Query · Tailwind · Vitest + Testing Library
- **Deploy:** API → Railway · Web → Vercel · CI/CD → GitHub Actions

## URLs de produção
- Web: https://tayro-app.vercel.app
- API: https://api-production-a813.up.railway.app (prefixo global `api/v1`)
- Railway Service ID: `f0afafae-1cd2-41e2-b462-8c413dd53209`
- **Railway: plano Hobby (pago, US$5/mês) desde 2026-07-05.** Trial de créditos grátis acabou silenciosamente (sem aviso) e derrubou a API em prod — `curl` na API respondia 404 do EDGE do Railway ("Application not found"), não do NestJS. Banco (Neon) não é afetado por isso, só o compute. Se a API cair de novo do nada, checar billing do Railway ANTES de investigar código/deploy.
- **Neon tem 2 branches: dev e prod, com hosts DIFERENTES.** `apps/api/.env` local aponta pro branch de **dev**. A `DATABASE_URL` de prod fica só no Railway (env do serviço), nunca no `.env` local — dev e prod NÃO compartilham o mesmo banco. Pegadinha: o branch de dev pode conter uma cópia real de dados de creators (ex: handles reais), então "ver dado real" ao consultar o `.env` local NÃO prova que é prod — já causou confusão (2026-08-03). Migrations locais (`prisma migrate deploy` manual) só afetam o branch de DEV; produção só recebe migration via CD.
- **CD vermelho com deploy OK — era o Railway CLI, não o build (RESOLVIDO 2026-08-22 — `--detach` + poll, ver abaixo). NÃO usar `@latest` no workflow.** Histórico do problema, 3 encarnações do mesmo falso-negativo:
  - *Flake de log-streaming (2026-08-03, runs #38/#40):* `railway up --ci` saía com `Failed to stream build logs` ~65s após o início, deploy completando normal. Fix na época: retry de 1 tentativa extra (v0.27.0/#74).
  - *Trava sem retornar (2026-08-15 → 2026-08-21):* a partir da **5.41.2** o CLI completava o build, publicava a imagem e **nunca retornava** — só morria no `timeout`, com a produção já atualizada. Workflow puxava `@railway/cli@latest`; um release do Railway quebrou o CD sem nenhuma mudança nossa (5.41.1 = último verde em 14/08; 5.41.2 e 5.42.x = 5 falhas seguidas). Tentativa de fix: **versão pinada** (5.41.1) no `cd.yml`.
  - **A versão pinada NÃO resolveu (2026-08-22):** o mesmo travamento aconteceu com a 5.41.1 pinada, num release que não tocava `apps/api` — o Railway respondeu "no changes detected in watch paths, build will skip" e o CLI travou do mesmo jeito, sem log pra transmitir. Ou seja, a causa nunca foi a versão do CLI — é o `--ci` (stream build logs, then exit) não detectar corretamente o fim do streaming, com ou sem build de verdade acontecendo.
  - **Fix real:** `up --detach` (sobe e retorna na hora, sem streaming) + poll pontual do status via `railway deployment list --json` até `SUCCESS`/`SKIPPED` (sucesso) ou `FAILED`/`CRASHED`/`REMOVED`/`REMOVING` (falha), com teto de 15min. Sem streaming longo, não tem como pendurar — e o job ainda falha de verdade se o build falhar (o que o `--ci` também garantia). Retry de 2 tentativas removido: não cobria nenhum dos dois modos de falha acima e só garantia +5min em toda falha real.
  - **Assinatura do problema antigo, pra reconhecer se voltar:** job durando ~10m30s cravado (300s + 300s + overhead) com as duas tentativas batendo no `timeout`. A migration roda no step ANTERIOR ao deploy — uma falha real dela ficava mascarada por esse vermelho crônico.

## CI/CD
- **CI** (`.github/workflows/ci.yml`): roda em todo PR — lint · typecheck · test · build (API e Web).
- **CD** (`.github/workflows/cd.yml`): no merge em `main`, após CI passar → migrations + deploy.
  - **O step de deploy tem teste: `bash .github/workflows/test/deploy-step.test.sh`.** Extrai o bloco `run:` do próprio `cd.yml` (não uma cópia, que envelheceria) e roda contra um `npx` falso, cobrindo os cenários que morderam de verdade: build concluído, build pulado por watch path, `FAILED`/`CRASHED`, upload falhando, id ausente na saída, JSON com wrapper diferente e status desconhecido. **Rodar antes de mexer nesse step** — ele só era exercitável mergeando na `main` e torcendo, e isso custou 6 releases vermelhas até a causa aparecer.
- **Git Flow:** `feature/* → develop → main`, sempre via PR. Branches protegidas (CI obrigatório + 1 aprovação, sem push direto/force-push) desde 2026-08-16 — **antes disso a proteção nunca tinha sido de fato aplicada no GitHub** apesar desta linha já afirmar isso; achado ao dar acesso Write a um colaborador externo (repo pessoal não suporta colaborador read-only, só Write). Config real fica no GitHub (Settings → Branches), não neste arquivo — conferir com `gh api repos/pssgarcia/tayro.app/branches/<branch>/protection` antes de assumir que está ativa.
- **SEMPRE partir da `develop` atualizada — regra nº 1, antes de qualquer código (MORDEU 2026-08-13).** `develop` tem que estar idêntica à produção antes de começar qualquer coisa. O primeiro comando de toda sessão de implementação é `git fetch origin`; depois conferir `git diff origin/develop origin/main --stat` (tem que vir **vazio** — os merge commits de release ficam só na `main`, então `git log` mostrar a develop "atrás" é normal, o que importa é o diff de conteúdo) e só então criar a branch a partir de `origin/develop`.
  - **Nunca confiar no CLAUDE.md, no snapshot de git status do início da sessão, nem em memória de sessão anterior** para saber a versão atual ou o que já foi entregue. Antes de afirmar que algo "não existe" / "não tem tela", verificar contra `origin/develop`, não contra a working tree.
  - O que custou ignorar isso: a `feature/campaign-lifecycle` saiu de uma base velha, a `main` já estava na v0.36.0 (não v0.30.0, como o CLAUDE.md local dizia), e o PR #104 saiu conflitado reimplementando "encerrar e apagar campanha" — que o #102 tinha entregue em produção 2 dias antes. Uma auditoria inteira do produto foi feita em cima de um retrato obsoleto e reportou como furo em produção coisas já corrigidas.

## Observabilidade (Sentry) — `D-20`
- **Inerte sem DSN.** `apps/api/src/instrument.ts` e `apps/web/src/instrument.ts` são o 1º import de `main.ts`/`main.tsx`. Sem `SENTRY_DSN`/`VITE_SENTRY_DSN` o `Sentry.init` não roda. **Em produção a API não sobe sem `SENTRY_DSN`** (`resolveSentryConfig` lança, mesma regra do `resolveAllowedOrigins`).
- **API:** `SentryGlobalFilter` (`APP_FILTER`, antes do `APP_GUARD` em `app.module.ts`) reporta o que **não** for `HttpException` e delega pro handler padrão do Nest — cliente segue com 500 genérico sem stack; 4xx mapeado nos services é fluxo de controle, **não vira evento, não há double-report**. A regra inviolável "stack trace nunca vaza pro cliente" continua valendo. Falha em background do `instagram-sync` (`scheduleRefresh`, roda depois da resposta HTTP) tem `Sentry.captureException` explícito no `.catch` — era invisível.
- **Web:** `Sentry.ErrorBoundary` no topo do `App` com `SentryFallback` (erro de render dava tela branca). `<Routes>` embrulhado por `withSentryReactRouterV7Routing` pra traces por rota.
- **Escopo:** erro não tratado + tracing amostrado a **10%** (`(VITE_)SENTRY_TRACES_SAMPLE_RATE`, default 0.1). **Session Replay DESLIGADO** — sem consentimento hoje, gravaria DOM de `/apply/:id` e `/auth/*` (LGPD). Não religar sem política de privacidade.
- **PII:** `sendDefaultPii: false` (sem IP) + `beforeSend` remove corpo de `/auth/*` e headers de auth nos dois apps. Sentry é **sub-processador** de dado pessoal (ver `roadmap.md` → LGPD Bloco 3).
- **Onde ficam os secrets:** Railway service env (`SENTRY_DSN`, `SENTRY_ENVIRONMENT`) pra API; Vercel project env (`VITE_SENTRY_DSN`, `VITE_SENTRY_ENVIRONMENT` + `SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT` só pro upload de sourcemap) pra web. **Nada em GitHub Secrets.** Região **EU** (DSN tem `ingest.de.sentry.io`).
- **Release:** git SHA automático — `RAILWAY_GIT_COMMIT_SHA` (API), `VERCEL_GIT_COMMIT_SHA` via `vite.config.ts` → `process.env.VITE_SENTRY_RELEASE` (web).
- **Sourcemaps:** web = `build.sourcemap: 'hidden'` + `@sentry/vite-plugin` (inerte sem `SENTRY_AUTH_TOKEN`) com `filesToDeleteAfterUpload` — **nenhum `.map` servido nem no precache do PWA**. API = `node --enable-source-maps` + os `.js.map` do tsc, **sem** upload. `cd.yml` não muda (upload roda no build remoto da Vercel).
- **Como testar em prod:** rota/botão de erro temporário → conferir evento no Sentry com stack legível + tags `environment`/`release` certas → remover o gatilho.

## Regras de segurança (invioláveis)
- Secrets SEMPRE em `.env` / GitHub Secrets. Nunca no bundle, logs ou erros HTTP.
- Guards em TODA rota autenticada. Endpoint público deve ser explícito e documentado.
- `@MaxLength()` em todo campo de texto livre (DTOs) — defesa contra payload spam/DoS.
- Refresh token invalidado no uso (rotation). Token roubado não pode mint forever.
- CORS com allow-list explícita.
- Throttle específico em `/auth/*` (5 req / 15 min por IP).
- Erros de infra (Prisma, HTTP externo) nunca vazam stack trace pro cliente.

## Princípios de colaboração
- **TDD obrigatório.** Humano decide O QUÊ; agente levanta flags e sugere proteções não pedidas.
- Small releases, refactoring contínuo, segurança como hábito.
- Pedro quer aprender TODO o ciclo (requisitos → produção) e ser desafiado.
- **Performance é obrigatória.** Em toda query Prisma: preferir `include`/`select` a loops (N+1). Toda operação check-then-act DEVE estar em `prisma.$transaction()`. Nunca aprovar/criar recursos sem atomicidade.

## Feito
- Segurança: refresh token rotation, campaigns visibility guard
- Segurança #3/#4/#5 (PR #20): throttle `/auth` (5/15min; ThrottlerGuard global que faltava), `@MaxLength` em TODOS os DTOs de texto livre (password=72 p/ bcrypt), CORS fail-fast se `ALLOWED_ORIGINS` ausente em prod. `ALLOWED_ORIGINS` corrigido no Railway (era localhost).
- Performance: detector de N+1 (`QueryCounter`) + fix de race conditions (approve com `$transaction` Serializable; creators trata P2002). `create()` era falso positivo (pending não conta p/ maxSpots)
- **P2 brand COMPLETA:**
  - Detalhe de campanha — 4 abas: Visão Geral (v0.2.0/#22), Conteúdos (v0.3.0/#23, endpoint `GET /submissions/campaign/:id` sem N+1), Recompensas (v0.4.0/#25, fluxo PENDING→ISSUED→DELIVERED; fix: create exigia conteúdo APROVADO → relaxado p/ application aprovada)
  - Dashboard (v0.5.0/#27): `GET /brand/dashboard` agregação sem N+1 (groupBy/count num único `$transaction`)
  - Cadastro de marca (v0.6.0/#29): `/register/brand` com auto-login
  - Perfil de marca (v0.7.0/#31): `GET/PATCH /brands/me` + `/brand/profile`
- **P3 creator — fundação (v0.8.0/#33, #35):** InfluencerLayout + InfluencerGuard, `/register/influencer` UI, "Minhas candidaturas" (status via `useMyApplications`). Fix: cadastro de creator com conflito de handle/email → 409 com discriminador `field` + msg amigável (antes P2002 não tratado → 500 → "Erro de conexão"); front mostra msg real inline, "sem conexão" só quando request não chega. Removido check-then-act de email (race fechada via `@unique`).
- **P3 creator — perfil (v0.9.0/#36):** API `GET/PATCH /influencers/me` (InfluencersController, flatten email, P2025→Forbidden) · DTO `UpdateInfluencerDto` (sem instagramHandle — imutável neste fluxo) · Frontend `/influencer/profile` (4 cards: identidade, sobre, perfil público LGPD toggle, conta; form só ativo se dirty; email read-only).
- **P3 creator — browse (v0.10.0/#39):** Frontend `BrowseProgramsPage` (grid responsivo, paginação com `keepPreviousData`, skeleton, empty state, erro) · `ProgramCard` (link `/apply/:id`, oferta em lime, nichos, deadline, vagas) · hook `useBrowsePrograms` · rota `/influencer/browse` como index.
- **Responsividade mobile-first (v0.11.0):** Todo o produto responsivo em 360/768/1024/1280px. InfluencerLayout + BrandLayout: sidebar `hidden md:flex`, mobile header (logo + logout) + bottom tab bar fixo com `env(safe-area-inset-bottom)`. CampaignDetailPage: tabs `overflow-x-auto shrink-0` (4 labels longos não cabem em 360px). Todas as telas: `px-4 sm:px-6`, `py-6 sm:py-8`, `text-xl sm:text-2xl`. Botões de auth `min-h-[44px]`. PublicApplyPage: `break-words` na descrição, `min-h-[44px]` no CTA. `viewport-fit=cover` no index.html.
- **Logo clicável + botão voltar (v0.12.0/#43):** Logo "tayro" em InfluencerLayout → `/influencer`, BrandLayout → `/brand`, PublicApplyPage → `/login`. Botão `← Voltar` (`navigate(-1)`) no header do PublicApplyPage.
- **Dashboard creator (v0.13.0/#45):** `/influencer/dashboard` como nova home (index redireciona para cá). 3 pills de resumo (candidaturas totais, aprovadas, recompensas a receber), últimas 4 candidaturas com StatusPill, recompensas PENDING+ISSUED. Nav do creator: 4 itens (Início, Programas, Candidaturas, Perfil). Hook `useMyRewards` + tipo `MyReward`.
- **Envio de conteúdo (v0.14.0/#47):** `/influencer/submissions` — lista de conteúdos com status (Em análise/Aprovado/Recusado/Revisar) + feedback da marca. Modal "Enviar conteúdo": seletor de candidatura APPROVED, URL, tipo (Reel/Vídeo/Foto/Story), legenda. MyApplicationsPage: cards APPROVED ganham link "Enviar conteúdo" → `/submissions?apply=<id>`. Nav: 5 itens (Início, Programas, Aplicações, Conteúdo, Perfil). Hooks `useMySubmissions` + `useCreateSubmission`, tipo `MySubmission`.
- **Minhas recompensas (v0.15.0/#49):** `/influencer/rewards` — lista de recompensas com tipo (Pagamento/Produto/Desconto), valor, StatusPill (A receber/A caminho/Entregue), notas e data de emissão. Pills de resumo no topo. Dashboard: pill "a receber" vira Link clicável para `/influencer/rewards`.
- **Integração Instagram real via RapidAPI (v0.16.0/#52):** `RapidApiInstagramProvider` — fluxo 2 passos (`GET /profile?username=` → `pk`+`follower_count`; `GET /feed?user_id={pk}` → posts). Profile obrigatório (2 retries, lança `InstagramFetchError`); feed best-effort (falha/privado → `recentPosts: []`, followers preservados). DI token `INSTAGRAM_PROVIDER` troca `stub`↔`rapidapi` via env.
- **Polimento do card de candidatura (v0.17.0/#55):** thumbnails maiores (`grid grid-cols-6` em vez de faixa `flex`), handle do IG vira link clicável (`ExternalLink` icon) pra marca abrir o perfil.
- **Foto de perfil do IG no card (v0.18.0/#56):** campo `igProfilePicUrl` (migration `add_ig_profile_pic_url`), mapeado de `hd_profile_pic_url_info.url` no provider, salvo pelo `InstagramSyncService`, exposto no `influencerSelect`.
- **Fix autocorrect + log de erro do sync (v0.19.0/#58):** iOS mutilava handles digitados (`__` virava `_`) por falta de `autoCorrect="off"` nos inputs de handle (`PublicApplyPage`, `RegisterInfluencerPage`). Log de erro do `InstagramSyncService` melhorado (mensagem real do erro, não só stack genérico).
- **Fix `@@handle` no sync (v0.20.0/#60):** handles inseridos fora do fluxo padrão (ex: edição direta no Neon) podiam ter `@` prefixado → sync construía URL com `%40handle` → API rejeitava. `InstagramSyncService.refresh()` agora normaliza (`replace(/^@+/, '')`) defensivamente antes de chamar o provider.
- **Fallback `profile_pic_url` (v0.21.0/#62):** `hd_profile_pic_url_info` nem sempre vem no response da API (depende da conta). Fallback: `hd_profile_pic_url_info?.url ?? profile_pic_url ?? null`.
- **Botão "Atualizar" no estado OK (v0.22.0/#64):** o refresh manual do IG só aparecia com `igFetchStatus=FAILED`. Cards em OK mas com dados velhos (foto ausente pré-fallback, thumbnails de CDN expirada) ficavam sem escapatória — backend já suportava `force:true` com cooldown de 15min, faltava só expor o botão também no estado OK. `RefreshButton` extraído e reusado nos dois estados.
- **Proxy same-origin pra foto de perfil do IG (v0.23.0/#65):** ver "Footgun CORP" na seção de decisões de domínio. `GET /ig/avatar/:influencerId` (público, `@SkipThrottle`) busca a foto server-side e re-serve com `CORP: cross-origin`. SSRF fechado: URL só vem do banco (nunca do cliente) + allow-list de host (`.cdninstagram.com`/`.fbcdn.net`, checado por sufixo) + timeout.
- **Apply autenticado direto do browse (v0.24.0/#67):** backend (`POST /applications`, role INFLUENCER) já existia — gap era só frontend. `ProgramCard` vira modal de confirmação (mensagem opcional) em vez de mandar a creator logada pro fluxo público `/apply/:id` (que criava conta duplicada). Fix junto: copy sem gênero assumido ("perfeita"→"ideal", "creator aprovada"→"candidatura aprovada").
- **E-mail de aprovação/recusa (v0.25.0/#69):** `EmailModule` novo — mesmo padrão do Instagram (interface `EmailProvider` + DI token `EMAIL_PROVIDER`, `stub`|`resend`). Disparo best-effort (nunca derruba approve/reject) em `ApplicationsService.approve()`/`.reject()`. `EMAIL_PROVIDER=stub` em prod até ter domínio verificado no Resend (Pedro decidiu comprar domínio real só no fim do MVP — sandbox do Resend só entrega pro próprio e-mail da conta).
- **Claim/set-password de conta CLAIMABLE (v0.26.0/#71):** `User.claimTokenHash`(`@unique`, hash SHA-256)+`claimTokenExpiresAt` (7 dias). Emitido na criação da conta (`CreatorsService.findOrCreateInfluencer`, sem N+1) + e-mail via `EmailService.sendClaimAccount`. Reaplicar com token ainda não usado reemite+reenvia automaticamente (fecha caso de link expirado). `POST /auth/claim` reaproveita `buildAuthResponse` — auto-login. Frontend `/claim?token=` (público, fora de guards). `StubEmailProvider` loga qualquer link do corpo do e-mail (fix v0.27.0/#73) — é a única forma de pegar o link do claim em dev sem configurar Resend de verdade.
- **Preview de identidade no Claim (v0.29.0):** `GET /auth/claim/:token` (novo, público, throttled) traz identidade (handle/e-mail/avatar/`influencerId`) + título da candidatura mais recente SEM consumir o token — ao contrário do `POST /auth/claim`. Fecha a limitação conhecida de link inválido/expirado só aparecer no erro do submit: agora `ClaimAccountPage` mostra a mensagem já na carga da página (401 do preview = mesma UX de token inválido). Se o preview falhar por outro motivo (rede/5xx, não 401), degrada pro form sem a placa de identidade — quem valida o token de verdade continua sendo o POST. Frontend: hook `useClaimPreview` (padrão de hook dedicado + mock em teste, não `useQuery` inline), avatar via o mesmo proxy `/ig/avatar/:influencerId` já usado no `ApplicationCard`.
- **Detalhe do programa para a creator (v0.31.0):** `/influencer/programs/:id` (`ProgramDetailPage`, dentro do InfluencerGuard) via `GET /campaigns/:id` (hook `useProgram`). Corrige fluxo invertido: "Ver programa" no Abertos abria DIRETO o modal de candidatura — agora navega pro detalhe (marca, oferta na placa, prazo/vagas, nichos, descrição, briefUrl) e a decisão ("Quero participar") acontece lá. `ProgramCard` virou navegação pura (`<Link>`, sem estado). `ApplyModal` extraído pra arquivo próprio (`pages/influencer/ApplyModal.tsx`) com `onApplied` → `/influencer/applications`. `PlateActionBar.primary` ganhou `to?` (renderiza `<Link>` com o visual idêntico ao botão — não duplicar estilo). Candidatura já existente (qualquer status, inclusive WITHDRAWN — unique `(campaignId, influencerId)` bloqueia re-apply com 409) mostra StatusPill + link em vez do CTA.
- **Perfil público da creator / media kit vivo (v0.32.0):** `/c/:handle` (`PublicCreatorProfilePage`, standalone como `PublicApplyPage` — sem layout compartilhado) via `GET /creators/:handle/public`. Fecha promessa quebrada: `ProfilePage.tsx` já prometia `tayro.app/c/{handle}` desde v0.9.0 sem a rota existir (caía no catch-all → `/login`). Hero = a placa (identidade: avatar via proxy `/ig/avatar/:id`, @handle, cidade, nichos, bio); stats fora da placa (seguidores/engajamento + `completedPartnerships` em destaque, mesmo padrão do dashboard); `ThumbGrid` extraído de `ApplicationCard.tsx` pra `components/primitives/` e reusado aqui. 404 uniforme pra handle inexistente E perfil privado (`publicProfileEnabled=false`) — já era assim no backend, front manteve a mensagem genérica (anti-enumeração). CTA final → `/register/brand`. Fix de backend junto: `getPublicProfile` não devolvia `id`/`igProfilePicUrl` — o avatar renderizava vazio pra 100% das creators (campo `avatarUrl`, que ninguém escreve, era o único exposto). OG tags: só estáticas/genéricas no `index.html` (SPA sem SSR — helmet client-side não gera preview em crawler de WhatsApp/Instagram); OG por creator fica pendente (ver "Pendente").
- **Vitrine pública de programas sem login (v0.34.0, roadmap.md AGORA #4):** `/programs` (`BrowseProgramsPublicPage`, standalone como `/apply/:id`) reusa `GET /campaigns` (mesmo endpoint de `/influencer/browse`, já era público) e o `ProgramCard` (`hrefBuilder` novo — default mantém `/influencer/programs/:id`). Visitante sem conta cai no card e vai pro `/apply/:id` **que já existia** (mostra oferta completa + form de candidatura, cria conta CLAIMABLE); creator já logada continua pro fluxo autenticado, sem duplicar conta nem ignorar candidatura existente. Não foi preciso criar uma tela de detalhe pública nova — `/apply/:id` já fazia esse papel, só não era encontrável sem o link exato de uma campanha. Link "Ver programas abertos sem criar conta" no `LoginPage`. Backend: `GET /campaigns` ganhou `OptionalJwtAuthGuard` (mesmo padrão de `GET /campaigns/:id`) só pra saber se o viewer é anônimo; `findAll`/`findOne` logam `anonymous=true|false` (contador cru, sem PII) — condição que o `/feature` pôs pra aprovar, já que não existe instrumentação de funil nenhuma no produto.
- **Retirar candidatura em qualquer pendente + confirmação (2026-08-14):** `PATCH /applications/:id/withdraw` só era alcançável na candidatura em destaque do `MyApplicationsPage` — as linhas da lista eram `<div>` inerte, então creator com 3 PENDING só conseguia retirar a mais recente. Agora toda linha PENDING tem "Retirar". **Achado ao ler a validação real antes de espalhar o botão:** retirar é DEFINITIVO e a consequência não é óbvia — o unique `(campaignId, influencerId)` não olha status, então `POST /applications` devolve 409 mesmo depois de WITHDRAWN (a creator fica trancada fora daquele programa pra sempre) e não existe rota de desfazer. A placa em destaque disparava isso **num clique, sem confirmação**. Agora as duas superfícies passam por `WithdrawModal`, que diz a consequência em vez de perguntar "tem certeza?", e fica aberto no erro pra dar retry.
- **Link do perfil público clicável (2026-08-14):** `ProfilePage` da creator prometia `tayro.app/c/{handle}` como TEXTO desde a v0.9.0; a rota existe desde a v0.32.0 e mesmo assim continuou não clicável. Agora `PublicProfileLink` com duas guardas que importam: (1) sem `instagramHandle` não promete endereço nenhum; (2) o link só aparece com o perfil público **já salvo no servidor** — `GET /creators/:handle/public` devolve 404 uniforme quando `publicProfileEnabled=false` (anti-enumeração), então linkar com perfil privado mandaria a creator pro 404. Lê de `profile`, **nunca do `watch()` do form**: com o toggle recém-ligado e não salvo, o backend ainda devolve 404. Abre em aba nova (não perde edição em andamento) + "Copiar link" com a URL absoluta (o que a creator manda pra marca).
- **Observabilidade via Sentry (`D-20`, 2026-08-27 — aguardando ratificação/env vars):** `@sentry/nestjs` + `@sentry/react`. Ver a seção "Observabilidade (Sentry)" acima. Entregue em 2 branches (`feature/observabilidade-sentry-{api,web}`), inerte sem DSN. Fecha 2 furos que existiam independente disso: API sem exception filter global, front sem error boundary. Motivador concreto: falha em background do `instagram-sync` era invisível (só log que "ninguém lê").
- Estrutura de testes (Jest + Vitest): **344 testes API + 419 web** (2026-08-27). **Toda tela tem teste: 31/31 arquivos em `apps/web/src/pages` têm `.spec.tsx` ao lado** — invariante nova, não deixar entrar tela sem teste.
- CI/CD completo + Git Flow + branch protection
- Deploy funcionando: API (Railway) + Web (Vercel) + migrations (Neon). Auto-deploy nativo do Railway DESLIGADO (Actions é o único gatilho); serviço "web" vestigial do Railway removido
- SPA routing fix (vercel.json), build do Railway com prisma generate
- Rename ugc-platform → tayro (código + Neon project)
- **PWA instalável (v0.33.0):** `vite-plugin-pwa` (`generateSW`, `registerType: 'autoUpdate'`) + manifest (`display: standalone`, cores da marca, ícones 192/512/512-maskable) + `apple-touch-icon`/meta iOS no `index.html`. Achado do `/architect`: em prod `/api/*` é rewrite same-origin pro Railway (`vercel.json`), não CORS — então a regra inviolável precisou ser mais ampla que só `/auth/*`: **nenhuma rota sob `/api/` pode ser cacheada**, autenticada ou não (dado de um usuário podia vazar pro próximo num device compartilhado). `runtimeCaching` com `NetworkOnly` explícito em `/^\/api\//` + `navigateFallbackDenylist` cobrindo a mesma regex — travado por teste (`vite.config.test.ts`, importa `pwaOptions` de `vite.config.ts` como objeto testável, não só SW gerado). Ícones são placeholder (ver "Pendente"). Fora de escopo desta v1, deliberado: offline-first (cache de resposta de API), prompt de update, botão de instalação customizado — sem evidência de usuário pedindo, reavaliar com tração real.
- **Redesign visual TAYRO 2a (v0.29.0, PR #80):** implementação do handoff `apps/web/design_handoff_tayro_2a/` (gitignored, referência local, não é código de produção). Dark-first (`#0A0A0A`), lime `#C6FF33`, "placa" (plate) como elemento de assinatura. Primitives novos: `Plate`, `CountUp`, `SegmentBar`, `StatusPill`, `StatBlock`, `ContentStatusPill`, `TabsUnderline`, `PlateField`, `PlateTextarea`, `PlateActionBar`, `NicheSelector` (variant `dark`|`plate`, tag quadrada — sem pill lime), `PlateEditField`/`PlateEditNiches` (row label+valor+chevron abre modal placa-formulário de campo único — padrão literal do mock pro "Editar" do Perfil da creator e da marca). 16 telas do handoff reconstruídas. Backend: `GET /campaigns/mine` ganhou `approvedCount`/`pendingCount` via `Promise.all` (groupBy quebra tipagem dentro de `$transaction` array form). Fix de bug: placa em destaque de Programas (`CampaignsPage`) só aparece em "Todas"/"Ativas", nunca em Rascunho/Encerradas (antes vazava pra qualquer aba). Tela da creator renomeada "Ficha"→"Perfil" (nav + título, mais claro pro usuário).
- **Fix: clique duplo no cadastro (creator e marca) pulava direto pro login (reportado 2026-08-11):** `PlateActionBar` reaproveita o mesmo `<button>` entre "Continuar" (`type=button`) e o passo final "Criar conta" (`type=submit`) — um clique duplo/toque rápido na mesma posição da tela caía no botão já convertido em submit e disparava o cadastro antes da creator/marca conseguir interagir com o passo de Nichos (chegava a renderizar por uma fração de segundo). Hook `useStepGuard(step)` desabilita o botão primário por 350ms a cada transição de passo — mesmo padrão aplicado em `RegisterInfluencerPage` e `RegisterBrandPage` (defeito idêntico nos dois, não isolado do creator). Delay zerado em `MODE=test` de propósito (`import.meta.env.MODE`) — o valor real (350ms) é coberto por teste dedicado do hook com fake timers (`useStepGuard.spec.ts`); nas páginas, o teste verifica só a fiação (botão desabilita quando o hook retorna `true`, via mock), sem depender de tempo real. **Correção adicional (PR #101):** a 1ª versão calculava a guarda num `useEffect` (roda só depois do commit) — abria 1 frame em que o botão já era `type=submit` mas `disabled` ainda não; confirmado com Playwright em Chromium real que um clique caía nessa janela. Corrigido calculando `guarding` no corpo do hook via o padrão oficial do React de "ajustar estado durante a renderização" (sem `Date.now()`/ref mutado no render — o React Compiler deste projeto proíbe via `react-hooks/purity`/`react-hooks/refs`).
- **Encerrar/apagar campanha (marca):** backend já tinha `PATCH /campaigns/:id/close` (ACTIVE→CLOSED) e `DELETE /campaigns/:id` (só DRAFT) com guarda de transição — gap era só de frontend. `CampaignHeader` (`CampaignDetailPage.tsx`) ganhou ação condicional por status: "Encerrar campanha" (ACTIVE) ou "Apagar rascunho" (DRAFT), CLOSED/COMPLETED sem ação (terminal). Modais `CloseCampaignModal`/`DeleteCampaignModal` seguem o padrão do `PublishModal` (placa + `PlateActionBar`, secundário=Cancelar). Hooks `useCloseCampaign`/`useDeleteCampaign` em `useCampaigns.ts`. Apagar mantém a regra de domínio (só DRAFT) — deletar campanha ativa/encerrada não existe, o caminho pra isso é encerrar. **Achados de bug ao testar com o app rodando de verdade (não só a suíte mockada):** (1) `usePublishCampaign` invalida `campaignKeys.detail(id)` local (`['campaigns', id]`), mas `useCampaign` (`useCampaignApplications.ts`) lê de `['campaign', id]` singular — chaves diferentes, invalidação nunca chega no cache real; mascarado no publish porque ele sempre navega pra uma página sem cache ainda. (2) `invalidateQueries` na chave certa não disparava o refetch da query ativa (campanha ficava ACTIVE na tela mesmo após o close retornar 200) — corrigido escrevendo a resposta do PATCH direto no cache (`setQueryData`) em vez de invalidar. (3) A resposta do PATCH `/close` não inclui `_count` (o GET completo sim) — sobrescrever o cache inteiro quebrava o header (`TypeError: Cannot read properties of undefined (reading 'applications')`); corrigido fazendo merge (`{...old, ...updated}`) em vez de substituir.

- **Publicar e editar rascunho pelo detalhe:** fecha o ciclo de vida que o "Encerrar/apagar" acima começou. Dois furos: (a) `usePublishCampaign` só era chamado no modal pós-criação do `NewCampaignPage` — quem salvava o rascunho e clicava "Agora não" ficava com o programa **preso em DRAFT pra sempre**, sem nenhuma outra tela que publicasse; (b) `PATCH /campaigns/:id` nunca teve tela, então nem um typo do título dava pra corrigir. `CampaignHeader` em DRAFT agora tem três saídas (Publicar programa · Editar · Apagar rascunho); ACTIVE segue só com Encerrar; CLOSED/COMPLETED terminal. `PublishCampaignModal` no padrão dos outros dois, e fica aberto em erro pra dar retry. Nova `/brand/campaigns/:id/edit` (`EditCampaignPage`) com `CampaignForm` extraído do `NewCampaignPage` e compartilhado pelas duas telas; schema + conversores centavos↔R$ e ISO↔yyyy-MM-dd em `campaignFormSchema.ts` (arquivo de componente não pode exportar helper — `react-refresh/only-export-components`). Só DRAFT edita: fora disso a tela não tenta salvar o que a API recusa com 400, explica o motivo. **Fix junto:** `usePublishCampaign` passou a escrever no cache com `setQueryData` como o `close` já fazia — o descompasso de chave que o #102 documentou deixava o status DRAFT na tela após o 200, agora que o publish acontece sem navegar.
- **Botão "Esqueci" morto removido do Login:** era clicável em produção sem `onClick`, sem rota e sem endpoint de recuperação de senha. Botão morto é pior que botão ausente. Volta junto com o fluxo real (ver "Pendente").
- **Identidade "Kinetic Editorial" — aba Fila (2026-08-16):** substitui o carrossel antigo (QueueTab/ApplicationCard, removidos) por uma direção visual nova, aprovada a partir de dois mockups do Pedro (screenshot + HTML literal). **Desktop** (`CampaignFilaTab.tsx`, dentro do BrandLayout de sempre — sem TopNav/Footer próprio, só o corpo da aba mudou): lista Pipeline com TODAS as candidaturas (qualquer status, não só PENDING — navegável, diferente do carrossel antigo que só mostrava quem esperava decisão) + placa clara "Kinetic" (crop marks em lime, fotos em p&b) com o detalhe da selecionada. **Mobile/tablet** (`CampaignPipelineMobileStory.tsx`): revisão em formato Story do Instagram — um candidato PENDING por vez em tela cheia (`fixed inset-0 z-50`, cobre a bottom tab bar do BrandLayout), navegação por toque nas laterais + swipe horizontal, swipe pra cima/"Ver posts" abre painel com nota da candidatura e feed sem perder a posição, aprovar/descartar avança automaticamente, barra de progresso em segmentos, tela de fim de fila com contagem. Largura travada em 480px em tablet (o hero em tela cheia esticava feio numa viewport de 800px+). Mutations e a query de candidaturas (com poll-while-PENDING: 6s enquanto alguém tem `igFetchStatus=PENDING`, desiste em 45s — mesma regra de sempre) são compartilhadas entre desktop e mobile, vivem só em `CampaignFilaTab`. "Match Score" e "Bio Note" do mock **não existem de verdade**: placeholder determinístico (hash do id) e a mensagem real da candidatura, respectivamente — decisão explícita de não fingir um cálculo real ainda. Paleta nova formalizada em tokens (`tailwind.config.ts` → `kinetic.*`: black/dark/gray/light/text/muted/border) em vez de hex solto, porque virou a **direção padrão do produto daqui pra frente** (convive com os tokens `plate`/`signal` do redesign 2a enquanto o resto das telas não migra).
- **Specs vivas (`specs/`, 2026-08-21):** processo, não produto — TAYRO passa a ter uma terceira categoria de arquivo além de `CLAUDE.md`/`knowledge/`: `specs/<slug>/spec.md` descreve o comportamento **atual** de uma capacidade de domínio, e **editar a spec é o pedido de mudança** (não documentação que se atualiza depois). `/architect` cria/mantém (capacidade nova exige veredito `AGORA` do `/feature`; capacidade existente não — é o atalho que faz valer a pena); `/review` ganhou um 5º eixo de deriva (código mudou comportamento sem atualizar a spec correspondente = achado). Retrofit completo das 13 capacidades de domínio reais (não uma por tela — dashboards que só agregam leitura ficaram de fora como spec própria): `campaign-lifecycle`, `applications-pipeline`, `campaign-fila-review`, `content-submissions`, `rewards`, `instagram-sync`, `brand-account`, `creator-account`, `account-claim`, `creator-discovery-and-apply`, `public-creator-profile`, `brand-dashboard`, `email-notifications`. Gerado a partir do código real (schema/controller/DTO), não do `CLAUDE.md` — achados concretos no processo: `Reward.value` é `String` livre, não `Int` em centavos (quebra a convenção de dinheiro do resto do produto); `CreateCampaignDto` não tem `@MaxLength` em nenhum campo de texto livre (contradiz a regra geral de segurança); `close()`/`remove()` de campanha e o núcleo do cooldown/staleness do Instagram sync não têm teste de unidade. **Refatorado no mesmo dia** pro padrão SDD (`Objective/Scope/Out of Scope/Domain/Behavior/API / Interfaces/UI Behavior/Acceptance Criteria/Error Scenarios/Known Gaps/Test Coverage/Current Implementation/Change History`, frontmatter `origin: RETROFIT|FEATURE` + `source_of_truth`) — separa requisito de implementação, converte regra vaga em critério verificável (`- [x]`/`- [ ]`), e distingue bug (`Known Gaps`) de comportamento pretendido (`Behavior`). Ver `specs/README.md`.

- **Dívida técnica das specs fechada (2026-08-23):** quatro `Known Gaps`/`[ ]` que o retrofit de specs tinha levantado e ninguém tinha fechado. (1) `@MaxLength`/`@ArrayMaxSize` em **todo** campo de texto livre que ainda estava sem teto — `CreateCampaignDto` (title 100, description 2000, briefUrl 2048, offerDescription 500, rewardValue 500, niches 20×50) e, por herança de `PartialType`, o de edição; `CreateRewardDto.value` (100); e o filtro `niches` de `ListCampaignsDto` (200), que é query string anônima fatiada com `.split(',')`. Era contradição direta com uma regra listada como inviolável neste arquivo. (2) `close()`/`remove()` de campanha ganharam teste de unidade — as duas transições destrutivas eram as únicas sem rede. (3) `registerBrand` deixou de fazer check-then-act de e-mail: confia na constraint `@unique` e traduz `P2002` em `409` com `field`, igual ao cadastro de creator (o corpo do erro melhorou junto — antes era a string genérica "Email already in use"). (4) A aba Fila saiu de **zero** cobertura pra 30 testes, com a query real em vez de hook mockado; o teste do teto de 45s do poll foi validado por mutação (desligar a proteção no componente faz o teste falhar), porque teste de timeout que passa com a proteção desligada é pior que teste nenhum. Specs correspondentes atualizadas no mesmo commit — `campaign-lifecycle`, `brand-account`, `campaign-fila-review`.

- **Robustez e cobertura (2026-08-23, mesma branch da dívida acima):** varredura atrás do que as specs **não** sabiam, depois de fechar o que elas sabiam. Achados e o que virou código:
  - **Decisão de candidatura podia ser sobrescrita em silêncio.** `approve`/`reject`/`withdraw` validavam `PENDING` na LEITURA e depois escreviam com `where: { id }` — sem repetir a condição. Aprovar e recusar quase simultâneos (dois dispositivos, ou retry de request cujo timeout venceu) resultava em: aprovação commitada, vaga consumida, e-mail "sua candidatura foi aprovada" enviado, e então o reject sobrescrevendo pra `REJECTED` sem erro nenhum — creator com o e-mail de aprovada no inbox, status recusado na tela e a vaga liberada sem ninguém perceber. Agora o status entra no `where` da escrita e o `P2025` vira **409** ("já foi decidida"), com o e-mail do perdedor não sendo enviado. Mesma classe de defeito existe na revisão de conteúdo (`content.service`) e **não** foi corrigida junto de propósito: mudaria contrato de erro da API, então entra como pedido de mudança próprio (registrado em `specs/content-submissions` → Known Gaps).
  - **Recompensa duplicada era irreversível.** `Reward` não tem unique `(influencerId, campaignId)`, `create` é check-then-act, e não havia `DELETE` — duplo clique ou retry gravava duas recompensas e as duas apareciam pra creator pra sempre. Decisão do Pedro: **múltiplas recompensas são legítimas** (dinheiro + produto na mesma campanha), então a resposta é desfazer, não unicidade. `DELETE /rewards/:id` só enquanto `PENDING` (a partir de `ISSUED` já foi anunciado à creator — apagar reescreveria o histórico dela) + ação "Remover" com modal que diz a consequência, no padrão do `WithdrawModal`.
  - **Cobertura:** `ContentService` foi de 1 método testado (de 7) pra todos — incluindo o gate de candidatura aprovada e os 3 caminhos de revisão. `InstagramSyncService` saiu de zero: staleness, `force`, preservação do último valor bom em falha, e o carimbo de horário mesmo em falha (o que faz o cooldown valer pro estado `FAILED` e não deixar o botão queimar cota). `PublicApplyPage` — única tela sem guard nenhum e que cria conta — ganhou 15 casos. `withdraw`/`refreshInfluencerIg` idem.
  - **Sem N+1 novo em lugar nenhum** (zero `await prisma` dentro de loop no backend inteiro) e nenhuma rota autenticada sem guard — os dois controllers sem `@UseGuards` (`creators`, `ig-avatar`) são públicos por decisão documentada.
  - **Fórmula de engajamento estava documentada errada aqui e na spec:** dizia `(likes+comments)/followers×100`, omitindo que a soma é dividida pelo NÚMERO DE POSTS. A fórmula descrita dava o dobro do valor real. Corrigida nos dois lugares (o código sempre esteve certo).

- **Cobertura total das telas (2026-08-23):** as 7 últimas telas sem teste ganharam spec, fechando a meta de 31/31. `RegisterChooserPage` (destino de cada saída — mandar creator pro fluxo de marca cria conta com papel errado e não existe troca de papel no produto) · `CampaignCard` (as duas variants, singular/plural, DRAFT sem contagem de candidatura, e a URL pública absoluta do "Copiar link", que é o que a marca manda pras creators) · `CampaignsPage` (filtro por status + a regressão da placa em destaque vazando pra abas de rascunho/encerradas) · `CampaignForm` (compartilhado por criar e editar: campos derivados do `offerType`, prévia da oferta ao vivo, e a conversão reais→centavos, que errada multiplica ou divide a oferta por 100) · `DashboardPage` da creator (taxa de conversão, "a receber" incluindo ISSUED, só as 3 candidaturas mais recentes) · `RewardsPage` (rótulos traduzidos de tipo e status) · `SubmissionsPage` (cascata da placa em destaque + modal de envio com os 3 caminhos de erro). Total: **351 testes web**.

- **Fix do CI + copy neutra + textarea que cresce (2026-08-23):**
  - **`npm run typecheck` no `apps/web` era um NO-OP e nunca checou nada** — `tsconfig.json` tem `"files": []` + apenas `references`, e `tsc --noEmit` sem `-b` não segue project references. Passou a ser `tsc -b --force`. O `--force` não é zelo: o `tsbuildinfo` vive em `node_modules/.tmp/`, que o Vercel guarda em cache entre builds, então dois erros de tipo pré-existentes ficaram anos-luz invisíveis até um arquivo novo forçar a recheca do grafo.
  - **`TabsUnderline` quebrava a inferência do genérico em 3 call sites.** `onChange` entrava na inferência de `T`; passar o setter de um `useState` (`Dispatch<SetStateAction<T>>`, que inclui a forma de callback) alargava `T` pra `string`. Corrigido com `NoInfer<T>` em `active`/`onChange` — `T` sai só de `tabs`. Não anotar tipo no call site: o conserto é no primitivo.
  - **`useApplications` tinha um `options` órfão** (`refetchInterval` tipado à mão) que não batia com a assinatura do TanStack e degradava `data` pra `unknown` no consumidor. O poll-while-PENDING vive em `CampaignFilaTab` desde 2026-08-16; ninguém passava options. Removido.
  - **Copy com gênero assumido, varrida em 6 arquivos** (reportado pelo Pedro em prod): "Oferta à creator" → "Oferta do programa"; "as creators aprovadas" → "as candidaturas aprovadas"; "Se aprovada, os detalhes…" → "Se a candidatura for aprovada…"; e mais 3. **Regra:** quando o sujeito é a PESSOA, reescrever pra falar do objeto (candidatura, programa, conteúdo) ou usar 2ª pessoa. "candidatura aprovada" pode ficar — ali o feminino é da palavra, não de quem se candidatou.
  - **`PlateTextarea` não crescia** (reportado com print): `rows={1}` fixo e sem `break-words` — texto mais longo rolava pra fora de um campo de uma linha e a pessoa perdia de vista o que tinha escrito. Agora auto-grow (`scrollHeight`, com `height: auto` antes de medir pra encolher ao apagar), `break-words` e ref encaminhada (o `register` do RHF manda uma). **Achado colateral:** o `leading-none` que estava lá **nunca foi aplicado** — `leading-*` e `text-[…]` são o mesmo grupo de conflito no `tailwind-merge`, e a classe de tamanho vinha depois. Entrelinha agora vai colada (`text-[14px]/[1.45]`). Se alguma classe do design "não pega", suspeitar do merge antes do CSS.

- **Fila em português + foto do IG em Entregas/Recompensas (2026-08-23):**
  - **Toda a Fila traduzida** (reportado com print de produção): status da candidatura na lista (`Pending/Approved/Rejected/Withdrawn` → `Pendente/Aprovada/Recusada/Retirada`), `Followers`, `Engagement`, `Recent Feed`, e o cabeçalho `Pipeline` → `Candidaturas`. `Bio Note` virou **"Mensagem da candidatura"** — nunca foi bio nenhuma, sempre mostrou `application.message`. **A regra de idioma no design system foi revertida** (ver seção Design system): inglês em label estrutural não é mais a identidade.
  - **`Match Score` ficou de fora da tradução de propósito.** É `hash(id)` mapeado pra 70–95, sem cálculo. Traduzir daria cara de métrica nativa a um número inventado, agravando a contradição com `vision.md` nº 5 ("nunca fabricar métrica... nada de score sem regra pública de cálculo"). Decisão de remover ou manter está com o Pedro.
  - **Entregas e Recompensas não mostravam foto nenhuma.** Liam só `avatarUrl` — campo que só a própria creator digita e que na prática está sempre vazio — então caíam nas iniciais pra praticamente todo mundo, enquanto a Fila mostrava a foto do IG. Pior em Recompensas: o `select` nem devolvia o `id` da creator, então o front não teria como pedir a foto. Corrigido nos dois lados (API expõe `id` + `igProfilePicUrl`; telas usam o proxy).
  - **Helper `creatorAvatarSrc` em `utils/format.ts`** — a regra "a foto do TAYRO é a do Instagram" estava copiada em 5 telas e eu ia criar mais 2. Agora é uma regra num lugar só, com teste travando que a URL da CDN **nunca** vai direto pro `src` (footgun CORP). `ClaimAccountPage` fica de fora de propósito: o preview do claim não expõe a URL, só um booleano — encaixá-lo exigiria vazar a URL.

- **Candidatura 500 na 1ª tentativa + sync do IG que nunca disparava (2026-08-24):** dois sintomas relatados pelo Pedro em produção, uma causa comum — efeito colateral acessório com poder de derrubar o caminho principal.
  - **`FRONTEND_URL` não estava definida no Railway.** `CreatorsService.sendClaimEmail` lê essa variável com `getOrThrow` **em tempo de request**, depois de `user.create` já ter commitado a conta e antes de `application.create`. Resultado: toda primeira candidatura de creator nova respondia 500 (com a conta já criada), e a segunda tentativa passava porque o handle já existia. Reproduzido na prod nos dois sentidos: `500` → depois de definir a variável, `201` de primeira. **Diferença que importa:** variável lida no construtor de um provider (RAPIDAPI_*, RESEND_*) já falha no boot e mata o deploy; lida dentro de um handler, ela passa pelo boot, pelo healthcheck e pelo smoke test, e só aparece na cara do usuário. Daí o `shared/config/required-env.ts` (mesmo padrão do `cors.ts`): fail-fast no boot em produção pras 5 variáveis lidas em request — `FRONTEND_URL` e as 4 do JWT (`JWT_ACCESS_EXPIRES_IN`/`JWT_REFRESH_EXPIRES_IN` não eram cobertas por NADA, e faltando qualquer uma delas todo login 500).
  - **O e-mail de claim virou best-effort de verdade.** O `EmailService.sendBestEffort` já existia, mas o `getOrThrow` acontecia um passo ACIMA dele, no `CreatorsService`. Agora `offerAccountClaim`/`sendClaimEmailBestEffort` capturam tudo: a candidatura é o evento de conversão do produto e não pode morrer por causa de um link.
  - **Só 1 dos 3 caminhos de entrada disparava o sync do Instagram.** `igFetchStatus` é anulável e o front trata `null` igual a `FAILED` (`CampaignFilaTab.tsx`), então: candidatura pública → `PENDING` + sync; **cadastro de creator (`registerInfluencer`) → `null` e sync nenhum**; **candidatura autenticada (`POST /applications`) → sync nenhum**. Creator que entrava pelo cadastro aparecia pra marca como "Dados do Instagram indisponíveis" desde o primeiro segundo, pra sempre, até alguém clicar "Atualizar" à mão. Era isso o "tem hora que vem de primeira e tem hora que não" — dependia da porta de entrada, não de instabilidade da API. **A RapidAPI foi verificada sã no mesmo dia** (profile 200, feed 200 com 36 posts, plano Basic ativo, cota em 10%) — não era ela. O disparo virou `InstagramSyncService.scheduleRefresh` (método público do serviço, não helper privado do `CreatorsService` — foi exatamente o helper privado que permitiu dois caminhos esquecerem dele) e os três chamam. O staleness de 24h já impede que isso multiplique cota; candidatura recusada não dispara nada.
  - **Chave da RapidAPI estava commitada** em `rapidapi.instagram.provider.spec.ts` — a chave REAL de produção, inteira, desde o commit `73243bc`. Trocada por `'chave-falsa-de-teste'` (nenhum teste ali faz rede, o `fetch` é mockado). **Está no histórico do git e precisou ser rotacionada** — remover do arquivo não desfaz o vazamento.
  - 22 testes novos na API (299 no total). O de `FRONTEND_URL` ausente reproduz o bug de produção literalmente: sem a correção, ele falha.

- **Imagens do Instagram guardadas por nós (D-18, 2026-08-23):** as URLs da CDN do Instagram são **assinadas e temporárias** — guardar o endereço não é guardar a imagem. Passado o prazo a CDN responde 403 e a foto sumia da Fila; não havia renovação agendada. Agora guardamos os **bytes** em `IgImage` (migration aditiva), tabela separada de propósito: `influencerSelect` roda em toda listagem da Fila e `Bytes` na linha da `Influencer` seria arrastado em cada consulta — tem teste travando isso (`ig-image-not-in-listings.spec.ts`). Rotas `/ig/avatar/:id` e `/ig/post/:id/:position` servem do banco, com **backfill no primeiro acesso** (sem script de migração). Guardar imagem é best-effort com `catch` PRÓPRIO — achado ao testar: estava dentro do `try` do sync, então falha de imagem marcaria `FAILED` um sync que deu certo, apagando dado bom por causa de uma foto (teste validado por mutação). Escolha de guardar no Postgres e não em storage externo: `D-A` (custo/fornecedor novo) e `D-E` (cascade resolve exclusão de graça) estão abertas — ver `decisions.md`. **API oficial do Instagram foi avaliada e recusada:** exigiria OAuth da creator e quebraria a candidatura espontânea sem conta (`D-17`), e também entrega URL temporária.

- **Validação do @ do Instagram antes de aceitar (D-19, 2026-08-27):** pedido do Pedro — "Usuário não encontrado" no formulário se o @ digitado não existir de verdade, pra evitar candidatura fantasma (conta criada, sem dado de Instagram, pra sempre). `/feature` recusou a versão ampla (bloquear a candidatura por um efeito acessório fere a Regra 10 de `creator-discovery-and-apply`); o pedido real, corrigido pelo Pedro na hora, é mais estreito — validar o **dado de entrada** no formulário, não um efeito colateral pós-envio. `/architect` desenhou: `GET /ig/handle/:handle` (público, três desfechos `FOUND`/`NOT_FOUND`/`UNKNOWN`, nunca dado de perfil), só `NOT_FOUND` bloqueia — `UNKNOWN` (provedor fora do ar, timeout, teto de cota) **libera** o envio de propósito (acusar falsamente custa mais que deixar passar um typo ocasional). Dispara no **blur do campo + submit**, nunca durante a digitação. `PublicApplyPage` e `RegisterInfluencerPage` (não o `ApplyModal` autenticado — sem campo de handle ali); no cadastro o bloqueio é mais crítico porque o handle é **imutável depois**. `IgProfileCache` em memória (TTL 15min, teto de entradas) reaproveita a consulta de perfil entre a verificação e a sincronização que vem logo depois — candidatura nova não paga a RapidAPI duas vezes pelo mesmo @. Throttle 10/60s por IP + teto global `IG_HANDLE_CHECK_BUDGET_PER_MINUTE` (default 60) — estourado, vira `UNKNOWN` sem chamar o provedor. **Mapeamento `404→NOT_FOUND` confirmado contra a API real via `curl`** antes do merge (corpo `"We're sorry, we couldn't find that."`) — achado no processo: 1ª tentativa usou handle de 38 caracteres (acima do limite de 30 do Instagram) e voltou `400`, não `404` — quase um falso sinal. `useInstagramHandleCheck` (hook compartilhado, dedupe por handle, falha de rede nunca bloqueia) nas duas telas. 36 testes novos na API (`IgProfileCache`, `StubInstagramProvider`, `checkHandle`/reaproveitamento no `RapidApiInstagramProvider` — validado por mutação —, `IgHandleController`) + 20 no web (hook + integração em `PublicApplyPage`/`RegisterInfluencerPage`). `decisions.md` `D-19`, `FIRME`.

- **Fixes mobile — link do IG na Fila + modal de recompensa + logo duplicado + tally 0/0/0 (2026-08-27):** bugs de produção no mobile. (−1) Resumo de fim de fila da revisão mobile (`CampaignPipelineMobileStory`) mostrava sempre `0 / 0 / 0`. O tally incrementava no `onSuccess` com **escopo do `mutate`**, que é sobrescrito quando a decisão seguinte sai antes de a anterior liquidar (o `invalidateQueries` do hook ainda revalidando segura o `isPending`, mas a candidatura já avançou e o botão reabilita). Reescrito: `decided: Record<id, 'approved'|'rejected'>` gravado no clique, tally derivado cruzando esse mapa com o `status` real na lista revalidada — só conta quem o servidor confirmou, sem contar em dobro. **As decisões approve/reject em si sempre funcionaram** (a fila esvaziava). (0) `NewCampaignPage`/`EditCampaignPage` renderizavam o próprio header com logo "tayro" + "Voltar" — mas vivem dentro do `BrandLayout`, que no mobile já mostra logo + Sair; dois "tayro" empilhados. Logo do header da página virou `hidden md:inline-block` (no desktop o BrandLayout só tem sidebar, então fica). (1) `CampaignPipelineMobileStory` mostrava o `@handle` só como texto — no desktop (`CampaignFilaTab`) ele é link pro Instagram desde sempre; agora é link (`target="_blank"` + `ExternalLink`) nas duas superfícies (spec `campaign-fila-review` atualizada). O `<a>` leva `pointer-events-auto` porque o overlay de identidade é `pointer-events-none` (deixa o toque passar pras zonas de prev/next). (2) O seletor de tipo (Monetária/Produto/Desconto) no modal "Registrar recompensa" era `flex` sem wrap → "Desconto" vazava do modal em 360px; agora `flex-wrap`. (3) O placeholder do campo Valor era ternário `MONETARY ? 'R$300,00' : 'Kit Whey 900g'` → DISCOUNT mostrava o placeholder de produto; virou `TYPE_CONFIG[type].placeholder` (Desconto = "Ex: Cupom AMANDA20 (20% off)"). Spec `rewards` atualizada.

- **Paridade mobile da Fila — ver aprovadas/recusadas no celular (2026-08-27):** avaliado no `/feature` junto com "cadastro manual de creator pela marca" (este **rejeitado** — cria registro sem consentimento, `vision.md` nº 3, e é decisão de estratégia adjacente a `D-B`; ver `decisions.md` → propostas rejeitadas). A Fila mobile (`CampaignPipelineMobileStory`) só tinha o Story `PENDING` — nenhuma superfície pra ver quem já foi aprovado/recusado no celular, assimetria com o desktop que feria `D-10`. Pedro puxou pra implementação imediata apesar de 0 marcas reais (o argumento foi `D-10`: mobile-first que esconde metade do dado é buraco, não backlog). Adicionado ao mobile um **alternador "Revisar" / "Todas"** no cabeçalho: "Revisar" = o Story de sempre; "Todas" = lista de toda candidatura (qualquer status) com rótulo em português, tocar numa linha abre o **mesmo** `CandidateStory` (barra Aprovar/Descartar gateada por `status === 'PENDING'`, decidida abre em leitura — igual à placa do desktop). `applicationStatusWord` extraído de `CampaignFilaTab` pra `utils/format.ts` (fonte única desktop+mobile). 8 testes novos (419 web). Spec `campaign-fila-review` atualizada.

- **Migração pro "Kinetic Editorial" — leva 1: primitivos + dashboards (2026-08-28):** a identidade aprovada em 2026-08-16 estava só na Fila — **2 arquivos** usavam `kinetic-*`, as outras 22 telas seguiam no 2a. A leva 1 tira a gramática de dentro da Fila (onde era markup solto: crop marks repetidas 4×, botão mono/uppercase 9× no desktop e 16× no mobile) e a transforma em **6 primitivos** em `components/primitives/kinetic/`: `KineticPlate` (crop marks em L de 16px em lime, `as` pra virar `<section>` quando é região de conteúdo), `KineticActions` (blocos retos edge-to-edge, primário lime — substitui o split bar do `PlateActionBar`), `StatusWord` (status vira palavra mono, sem pill), `StatFigure` (rótulo mono EM CIMA do número, invertendo o `StatBlock`), `KineticRow` (a linha vira alvo: selecionada ganha `kinetic-dark` + borda) e `KineticSegments`. 34 testes novos.
  - **Decisão de arquitetura:** primitivos NOVOS ao lado dos 2a, nunca `variant="kinetic"` nem migração no lugar — `Plate` é usado em **21 telas** e `PlateActionBar` em 13, então mexer neles mudaria de uma vez telas fora da leva. No fim da migração apaga-se a pasta, em vez de desfiar condicional.
  - **Vocabulário de status unificado:** `campaignStatusWord` e `contentStatusWord` saíram de dentro de `StatusPill.tsx`/`ContentStatusPill.tsx` e foram pra `utils/format.ts`, ao lado do `applicationStatusWord` que já morava lá. A candidatura passa a dizer **"Pendente"/"Aprovada"** também pra creator (o 2a dizia "Análise"/"Fechada") — as mesmas palavras que a marca já lê na Fila.
  - **Rótulo mono sobe pra caixa alta por CSS (`uppercase`), nunca no texto do JSX** — o DOM continua com o texto em minúsculas, então busca por texto em teste e leitor de tela não mudam. Foi o que permitiu migrar o dashboard da marca com os 7 testes existentes intactos.
  - **Fila refatorada pros primitivos** (desktop e o modo "Todas" do mobile) — senão ela ficava fora do sistema que ela mesma originou. Efeito colateral aceito e visível: a barra Aprovar/Descartar deixou de ser dois botões com respiro e virou dois blocos colados, como no resto do sistema.
  - **Dois testes do dashboard da creator estavam presos a `previousElementSibling`** — quebraram porque o Kinetic inverte a ordem rótulo/número. Reescritos com consulta escopada (`within(bloco)`), que sobrevive a mudança visual. Os outros 452 passaram sem toque.
  - Falta: abas Briefing/Entregas/Pagamento da marca (leva 2) e telas da creator (leva 3).

- **Migração pro "Kinetic Editorial" — leva 2: abas da marca (2026-08-28):** `/brand/campaigns/:id` inteiro passa pro Kinetic — header, barra de abas, Briefing, Entregas e Pagamento (a Fila já estava). Dois primitivos novos: `KineticTabs` (rótulo mono caixa alta, `overflow-x-auto` porque 4 abas em mono não cabem em 360px) e `KineticFact` (rótulo mono + valor em TEXTO — irmão do `StatFigure`, que é pra número). `StatusWord` ganhou `kind="reward"`, e `rewardStatusWord` saiu do `STATUS_CONFIG` local pro `utils/format.ts`.
  - **Briefing e Entregas nunca chegaram a seguir nem o 2a.** As duas ainda usavam `rounded-xl border bg-card p-5` como container, pills `bg-yellow-500/10 + border-yellow-500/20` e botão `bg-blue-600` — três coisas que o `DESIGN.md` lista explicitamente como "Don't". Não era dívida do Kinetic, era dívida anterior que a migração pagou.
  - **Entregas virou lista + placa**, o par que a Fila provou: a lista mostra toda entrega do filtro e a placa abre a selecionada com espaço pra legenda e feedback. No celular a lista vem ANTES da placa no DOM, pra que tocar numa linha atualize uma placa que já está no campo de visão. A seleção acompanha o filtro — se a entrega aberta sai do filtro, a placa passa pra primeira que restou. Spec `content-submissions` atualizada no mesmo commit (é mudança de comportamento, não só de pintura).
  - **Pagamento NÃO virou lista + placa, de propósito.** Cada recompensa tem ação própria e rápida (emitir / confirmar entrega / remover); obrigar a selecionar antes de agir seria um clique a mais por item. Ficou em blocos com ação inline, e a placa da tela é o modal de registro.
  - **Regressão evitada:** os dois fixes mobile de 2026-08-27 no modal de recompensa (`flex-wrap` no seletor de tipo, `placeholder` vindo do `TYPE_CONFIG[type]`) foram preservados de propósito na reescrita, com comentário no código dizendo por quê — reescrever modal é exatamente onde esse tipo de correção some.
  - **Dois testes de Entregas precisaram de escopo.** Com lista + placa, a entrega selecionada aparece nas DUAS superfícies: `getByText('Ana Creator')` passou a achar 3 nós onde esperava 2. Resolvido com `within(getByRole('list', { name: 'Entregas' }))`, e não afrouxando a asserção. Pelo mesmo motivo, avatar de linha é decorativo (`alt=""`) e só o da placa carrega o nome.
  - 465 testes web verdes (49 arquivos). Falta a leva 3: telas da creator.

- **Migração pro "Kinetic Editorial" — leva 3: telas da creator (2026-08-28):** fecha a migração combinada. `MyApplicationsPage` (Registro), `SubmissionsPage` (Entregas), `RewardsPage` (Recompensas) e `ProgramDetailPage` saem do 2a.
  - **Botão morto encontrado e consertado:** a placa em destaque do Registro tinha "Enviar conteúdo" **sem `onClick` e sem rota** — clicável em produção, sem fazer nada. O destino sempre existiu (`SubmissionsPage` lê `?apply=` e já abre o modal na candidatura certa); a fiação se perdeu no redesign 2a. Agora é `to={/influencer/submissions?apply=<id>}`. O teste cobria só a existência do botão, não o destino — por isso passou despercebido.
  - **`RewardsPage` e `ProgramDetailPage` não tinham primitivo nenhum** (`rounded-xl border bg-card`, pills `bg-amber-500/10`), e a `ProgramDetailPage` era a única tela do produto com **zero** classes responsivas.
  - **Vocabulário de status: uma exceção deliberada.** `creatorRewardStatusWord` (novo em `utils/format.ts`) diz o MESMO status da ótica de quem espera — "A receber"/"A caminho" em vez de "Pendente"/"Emitida". Não é drift: pra marca `PENDING` é trabalho não feito, pra creator é dinheiro não recebido. Por isso a tela de recompensas da creator **não** usa `StatusWord`, que carrega o vocabulário da marca. As abas do Registro, essas sim, foram alinhadas ("Fechadas" → "Aprovadas").
  - **Dois testes presos a `previousElementSibling`** (resumo de recompensas) reescritos com consulta escopada, mesma correção da leva 1 — o Kinetic inverte a ordem rótulo/número.

- **Migração pro "Kinetic Editorial" — leva 4: programas da marca (2026-08-28):** `/brand/campaigns`, `CampaignCard` (linha + placa em destaque), `CampaignForm` (compartilhado por criar e editar), `NewCampaignPage`, `EditCampaignPage` e os **3 modais de ciclo de vida** do `CampaignDetailPage` (publicar/encerrar/apagar) — estes últimos tinham escapado da leva 2, que migrou só o header e as abas.
  - Sem primitivo novo: a leva inteira saiu de `KineticPlate`/`KineticActions`/`KineticRow`/`KineticSegments`/`KineticTabs`/`StatFigure`/`StatusWord`, que é o sinal de que o conjunto extraído na leva 1 estava certo.
  - **Cap de segmentos replicado:** a placa em destaque de Programas usava um segmento por vaga, igual à aba Briefing — acima de 12 vagas viram tiras de 2px. Mesma regra dos dois lados (proporcional acima de 12).
  - Os 167 testes de `pages/brand` passaram sem alteração nenhuma.

## Convenção de release (develop → main)
- Título: `release: vX.Y.0 — <desc>` (SemVer pré-1.0; features de produto incrementam o minor)
- Corpo: changelog (`## O que vai pra produção` + `## Migrations`)
- PR de release é MANUAL — não abre ao mergear feature no develop. Última mergeada: **v0.49.0 (#145, 2026-08-27)**; **v0.50.0 aberta (#147, "paridade mobile da Fila")**, aguardando merge. Esta linha vive desatualizada (já apontou v0.30.0, v0.34.0, v0.36.0, v0.43.0 e v0.48.0 com a produção bem mais adiante, e chegou a ter a mesma frase colada duas vezes); **confirmar com `gh pr list --base main --state merged --limit 5` antes de numerar uma release**, nunca ler daqui e confiar.
- **SEMPRE usar merge commit, NUNCA squash** em releases. Squash cria commit sem ancestral comum → divergência de histórico → conflitos em futuros PRs. v0.8.0 usou squash (exceção pra limpar atribuição) e exigiu `git merge -s ours` em chore/sync-main-into-develop (PR #38) pra reconciliar.
- Releases antigos #1–#15 (infra/CI/CD) ficaram sem versão (pré-v0.1)

## Pendente
- **Gaps do MVP (levantamento 2026-08-03) — TODOS FECHADOS:** apply autenticado (v0.24.0) · e-mail de decisão (v0.25.0) · claim/set-password (v0.26.0) · preview de identidade no Claim (v0.29.0). Ver "Feito" pra detalhe de cada um.
  - Limitação conhecida, aceita por ora: sem reenvio manual de link de claim perdido (só reemite se reaplicar a um programa).
- **Ícones PWA são placeholder** (monograma "T" lime/dark gerado, não é o mark oficial do TAYRO — produto só tem wordmark texto hoje). Trocar `apps/web/src/assets/pwa-icon.svg` e rodar `npx pwa-assets-generator` de novo quando houver logomark definitivo. `public/favicon.svg`/`icons.svg` antigos (roxos, off-brand) ficaram órfãos — não referenciados em lugar nenhum, podem ser removidos.
- **Furos de ponta a ponta — auditoria de toda rota da API contra o que o front consome (feita 2026-08-13, REVALIDADA contra o develop em 2026-08-14, pós-v0.37.0):**
  - **Recuperação de senha não existe** (nem `POST /auth/forgot-password` nem tela). Quem esquece a senha fica fora do produto pra sempre — `POST /auth/claim` define senha uma vez e acabou. O botão "Esqueci" foi REMOVIDO do Login até o fluxo existir (v0.37.0). **Bloqueado por infra, não por código:** `EMAIL_PROVIDER=stub` em prod, então o e-mail não chegaria em ninguém — decidir o domínio no Resend ANTES de implementar, senão o fluxo fica pronto e inerte.
  - **LGPD:** sem trocar senha/e-mail logada, sem deletar conta, sem exportar dados, e **sem política de privacidade / termos / captura de consentimento** em lugar nenhum. Levantamento completo (o quê e por quê, com a ordem sugerida) em `.claude/knowledge/roadmap.md` → "LGPD"; a decisão de apagar vs. anonimizar conta é a `D-E` em `decisions.md`, ABERTA. Trocar senha é o mais urgente e é **segurança antes de LGPD**: hoje senha vazada não tem como ser trocada.
  - **Marca não descobre creators.** CRM "creator-first" sem busca/listagem: o controller `creators` só expõe `:handle/public`. A marca só vê quem se candidatou — `/c/:handle` só é alcançável por link direto que a creator mande (o link em si já é clicável no perfil dela desde 2026-08-14, ver "Feito"). É a maior lacuna de produto que sobra; passar pelo `/feature` antes de virar código.
  - **`GET /submissions/application/:applicationId` não tem consumidor nenhum** no front (ownership dupla brand-ou-creator implementada e testada, sem uso).
- **Dívida de modelo de dados sem superfície — decisão: NÃO mexer por ora, dropar tabela é irreversível e não urge:**
  - **`Notification`** — tabela com índice composto e ZERO referências em todo o código (API e web). Migration morta.
  - **`PartnershipResult`** — meio ligado, o que confunde mais que não ligado: lido em `getPublicProfile` e devolvido como `results[]`, tipado em `types/api.ts`, e o `PublicCreatorProfilePage` nunca renderiza. Não existe endpoint de escrita → o array é sempre `[]` e `visibleToCreator` nunca é setada por ninguém.
  - **`CampaignStatus.COMPLETED`** — estado inalcançável (nada transiciona pra ele). Só aparece no contador do dashboard, sempre 0.
- **Link "Ver programas abertos" no `LoginPage` (v0.34.0) ficou genérico visualmente** — testado em prod (2026-08-11), Pedro não gostou de como ficou embaixo da opção de login de marca (botão sem destaque, some no meio da tela). Revisitar posicionamento/estilo quando for polir essa tela; não é bloqueio funcional.

## Bugs conhecidos (backlog)
- **Imagem do IG some depois de um tempo — URL de CDN expirada (rediagnosticado 2026-08-23, corrige o registro anterior):** o que estava escrito aqui até hoje ("IG não vem completo na 1ª candidatura") estava **errado**. Pedro confirmou: os dados chegam completos e corretos na 1ª candidatura; o que quebra é a imagem **depois**, com o tempo. Causa verificada no código: guardamos a **URL assinada** da CDN (`igProfilePicUrl` e o `thumbnail` de cada post), não a imagem. A assinatura expira, a CDN passa a responder 403, e a foto some — no proxy `/ig/avatar/:id` (vira 404) e, pior, nas thumbnails do feed, que o front carrega direto da CDN sem proxy. **Não existe renovação agendada:** os únicos chamadores de `InstagramSyncService.refresh()` são a candidatura (`CreatorsService.findOrCreateInfluencer`) e o botão "Atualizar" manual — então a janela de validade de 24h nunca chega a ser avaliada em candidatura antiga que ninguém reabre. **Trocar pela API oficial do Instagram NÃO resolve isso** (a API oficial também entrega URL de mídia temporária) — o que resolve é parar de guardar URL e passar a guardar/cachear a imagem. Ver `specs/instagram-sync/spec.md` → Known Gaps. A API oficial continua valendo a discussão por outros motivos (ToS, consentimento/LGPD, métricas reais pro "histórico verificado") — é assunto de `/feature` + `/architect`, não de correção de bug.

## Design system
- Dark-first. BG #0C0C0C · surfaces #161616/#1E1E1E · borders #2C2C2C/#3A3A3A
- Lime #C6FF33 → Tailwind bg-lime/text-lime; shadcn --primary: 77 100% 60% (use o hex pra não ter drift)
- Accent quente #FF7A50 (recompensas/achievements) · frio #5B8EFF (links/ações 2árias)
- Texto #F0F0F0 / secundário #888888 · Sucesso #1EDB8C / Erro #FF4D4D
- Fontes: Space Grotesk (display/headings) · Inter (body). Stats com tabular-nums.
- CARDS, nunca tabelas. Avatares proeminentes. StatusPills coloridas. Copy encorajadora.
- **Redesign 2a (v0.29.0):** substitui os tokens acima em todas as telas migradas. BG `#0A0A0A`, lime `#C6FF33` mantido, "placa" `#E8E8E3` (card claro com crop marks) como elemento de assinatura — no máx. 1 por tela. JetBrains Mono (mono, máx. 3 usos/tela) somado a Space Grotesk/Inter. Ver `apps/web/design_handoff_tayro_2a/` (gitignored) pra spec completa. 6 regras do sistema: máx. 3 labels mono/tela, máx. 2 divisores/tela, sem efeitos empilhados, 1 "orçamento" de lime/tela, 1 placa/tela, piso de contraste no texto secundário.
- **"Kinetic Editorial" (2026-08-16) — direção padrão daqui pra frente**, substitui gradualmente o redesign 2a (que fica intacto nas telas ainda não migradas, não apagar os tokens `plate`/`signal` até migrar tudo). BG `#121212` (`kinetic-black`), superfícies `#1a1a1a`/`#2a2a2a` (`kinetic-dark`/`kinetic-gray`), texto secundário `#d1d1d1`/`#888888` (`kinetic-text`/`kinetic-muted`), borda de controle `#3a3a3a` (`kinetic-border`) — tokens em `tailwind.config.ts` → `colors.kinetic`. Lime `#C6FF33` continua sendo o MESMO token `lime` (era "kinetic-accent" no mock, sem duplicar). Placa clara vira `kinetic-light` (`#e5e5e0`, mais quente que o `plate` da 2a) com crop marks EM LIME (não neutros como na 2a) e fotos em p&b só sobre ela — fora da placa (hero em tela cheia no mobile, por ex.) foto fica a cores. Tipografia mais ousada que a 2a (títulos até `text-7xl`, stats até `text-6xl`). Botões primário/secundário como blocos retos lado a lado (não o split-bar `PlateActionBar` da 2a). **Copy é TODA em português — regra revertida em 2026-08-23.** Até essa data este arquivo dizia que misturar inglês em labels estruturais ("Pipeline", "Followers", "Bio Note", "Recent Feed") era decisão deliberada vinda dos mockups. **Não é mais:** o Pedro viu a Fila em produção e pediu tudo em português. Quem for mexer na Fila, não "restaure" o inglês achando que é a identidade — não é. O que sobrou de inglês é só nome de token e de variável no código, que não aparece pra ninguém. Primeira aplicação: aba Fila de `/brand/campaigns/:id` (ver "Feito"). **A partir de 2026-08-28 a gramática mora em primitivos (`components/primitives/kinetic/`) e está escrita por extenso em `apps/web/DESIGN.md` → "Kinetic Editorial — a direção atual"** (tokens, os 6 primitivos, as 6 regras e o estado da migração). Não recriar placa, botão ou status na mão: se falta variante, ela nasce no primitivo.

## Decisões de domínio — não quebrar
- offer* em Campaign (offerAmount[centavos], offerType CASH|PRODUCT, offerDeadlineDays, offerDescription) = fonte de verdade da oferta. rewardType/rewardValue DEPRECATED — não escrever, não expor.
- Dinheiro SEMPRE em centavos (Int). Nunca float. Exibe com Intl.NumberFormat pt-BR/BRL.
- Campaign.status = máquina de estados DRAFT→ACTIVE→CLOSED→COMPLETED. Publicar é transição guardada (só DRAFT publica), não flag. Ativar deixa /apply/:id vivo.
- maxSpots = vagas para creators APROVADAS ≠ total de applications. Não confundir no UI.
- Instagram: interface InstagramProvider + DI token INSTAGRAM_PROVIDER. Stub determinístico = default dev; RapidApi via env (mapeamento isolado, sem hardcode). Staleness 24h. Cooldown refresh-ig 15min carimbado em TODA tentativa (inclusive falha — senão FAILED é spammável e queima cota). Falha preserva último valor. calcEngagementRate = média de (likes+comments) POR POST / followers × 100, arredondada a 1 casa (função pura testada) — não é a soma das interações, somar inflaria a taxa. `igProfilePicUrl` = `hd_profile_pic_url_info.url ?? profile_pic_url ?? null` (v0.21.0).
- **Footgun CORP (JÁ NOS MORDEU, não repetir):** Instagram serve fotos de PERFIL (path `t51.82787-19`) com `Cross-Origin-Resource-Policy: same-origin` — browser bloqueia num `<img>` cross-origin, mesmo com URL válida e pública. Thumbnails do FEED (`t51.71878-15`) vêm com `cross-origin` e carregam normal — política diferente por tipo de mídia, mesma CDN. Não é contornável no frontend (nem `referrerPolicy` nem `crossorigin` resolvem). Fix: proxy same-origin (`GET /ig/avatar/:influencerId`, v0.23.0) — servidor busca a imagem (CORP não vale server-to-server) e re-serve pelo nosso domínio. Se algum dia a foto de perfil sumir de novo, ANTES de suspeitar do dado, inspecionar os response headers da URL da CDN.
- Perfil público de creator: publicProfileEnabled default false (LGPD). Só via GET /creators/:handle/public quando true.
- /apply/:id: cria conta creator leve CLAIMABLE (senha aleatória, ninguém sabe), chaveada por handle+email real. Claim via POST /auth/claim (v0.26.0) — ver "Feito".

## Auth no frontend — footguns (JÁ NOS MORDEU, não repetir)
- accessToken em Zustand (memória), NUNCA localStorage/sessionStorage. Refresh em cookie httpOnly.
- Silent refresh no boot do AppShell; guards avaliam SÓ depois do refresh resolver (senão race → /login).
- Interceptor de 401 NÃO usa window.location (loop de reload). Usa navigate do router, máx 1 redirect.
- /auth/refresh e /auth/login ISENTOS do retry de refresh do interceptor (senão loop infinito — foi o bug do login travado).
- Poll-while-PENDING (teto ~45s) pra dados de IG assíncronos.

## Telas prontas (frontend) — não reconstruir
- /login + /register/brand (AuthLayout; /register → /register/brand) · BrandLayout (sidebar Dashboard/Programas/Perfil) + BrandGuard
- /brand/dashboard (DashboardPage — **Kinetic**: placa com o número de candidaturas na fila + Resumo em 4 `StatFigure`, lado a lado no desktop, empilhado no celular)
- /brand/campaigns/:id → 4 abas, **todas em Kinetic** desde 2026-08-28 (header com ações derivadas do status: "Encerrar campanha" se ACTIVE; "Publicar programa" + "Editar" + "Apagar rascunho" se DRAFT; nenhuma se CLOSED/COMPLETED):
  - Fila (identidade "Kinetic Editorial", ver "Feito"): `CampaignFilaTab.tsx` — desktop lista Pipeline (toda candidatura, qualquer status) + placa clara com detalhe/approve/reject; `CampaignPipelineMobileStory.tsx` — mobile com alternador **"Revisar"** (Story em tela cheia, só PENDING, toque/swipe, painel "Ver posts", fim de fila com contagem) / **"Todas"** (lista de toda candidatura com rótulo de status → detalhe reusa `CandidateStory`; decidida abre em leitura). 3 estados IG (PENDING/FAILED+retry/OK) nos dois, poll-while-PENDING (6s/45s) compartilhado
  - Visão Geral (CampaignOverviewTab) · Conteúdos (CampaignContentTab, aprovar/recusar/revisão) · Recompensas (CampaignRewardsTab, modal de registro + issue/deliver)
- /brand/campaigns (lista+filtro) · /brand/campaigns/new (form rhf+zod, toggle CASH/PRODUCT, centavos, modal publicar + link /apply/:id) · **/brand/campaigns/:id/edit** (EditCampaignPage — mesmo `CampaignForm` do "novo", prefill via `campaignToFormValues`; fora de DRAFT não mostra form, mostra o motivo)
- /brand/profile (ProfilePage: rows label+valor+chevron abrem modal placa-formulário por campo — PlateEditField/PlateEditNiches, padrão 2a; email read-only, salvar só habilita se dirty)
- /apply/:id (página pública: oferta, form, estados 201/409/429) — responsivo (px-4 sm:px-6, min-h-[44px] no CTA, break-words na descrição)
- **/programs (v0.34.0):** vitrine pública, sem guard — mesma listagem de `/influencer/browse`, card leva pro `/apply/:id` se anônimo ou pro detalhe autenticado se já houver sessão de creator. Linkado do `LoginPage`.
- **Creator (v0.8.0):** InfluencerLayout + InfluencerGuard · /register/influencer (rhf+zod, NicheSelector, erro inline por `field` vindo do 409) · /influencer (minhas candidaturas via `useMyApplications`)
- **Creator (v0.9.0, renomeada "Ficha"→"Perfil" no redesign 2a):** /influencer/profile (rows label+valor+chevron abrem modal placa-formulário por campo — PlateEditField/PlateEditNiches; toggle LGPD inline; dirty gate; email read-only)
- **Creator (v0.10.0, revisto na v0.31.0):** /influencer/browse (placa em destaque + rows; ProgramCard só navega, não candidata) · /influencer/programs/:id (detalhe: oferta na placa, prazo/vagas, nichos, descrição; CTA "Quero participar" abre o ApplyModal; se já houver candidatura, StatusPill + link)
- **Creator (v0.13.0):** /influencer/dashboard (nova home/index) — 3 pills de resumo, últimas 4 candidaturas, recompensas PENDING+ISSUED. Nav 4 itens.
- **Creator (v0.14.0):** /influencer/submissions — lista com status + feedback, modal "Enviar conteúdo". Nav 5 itens (+ Conteúdo).
- **Creator (v0.15.0):** /influencer/rewards — lista de recompensas (tipo, valor, StatusPill, notas, data de emissão).
- **Claim (v0.26.0, preview de identidade v0.29.0):** /claim?token= (AuthLayout) — placa confirma quem é (avatar/@handle/e-mail) e o programa da candidatura antes do form de senha (`useClaimPreview`), link inválido/expirado detectado já na carga (não só no submit), auto-login, erros 401/429/sem-conexão.
- Helpers compartilhados em `utils/format.ts`: formatCurrency, formatDate, formatOffer, creatorAvatarSrc, **publicUrl/publicUrlLabel** (não duplicar)
- **Domínio público NUNCA literal no código.** Todo endereço que sai do produto (link de candidatura que a marca divulga, link do perfil da creator) sai de `publicUrl(path)` / `publicUrlLabel(path)`, que derivam de `window.location.origin`. **MORDEU 2026-08-24:** o código montava `https://tayro.app/...` em 3 telas — domínio **inexistente** (NXDOMAIN, verificado). Todo link que uma marca copiou pra divulgar programa desde a v0.29.0 estava morto, e a creator que mandava o link do perfil pra marca idem. Nenhum teste pegou porque nenhum teste sai da máquina; a proteção agora é o teste travar a ORIGEM como fonte, não o domínio como valor. Quando/se o `tayro.app` for comprado e apontado pra Vercel, nada de código muda.

## Convenções de trabalho (somar às de performance)
- **Mudar comportamento de uma capacidade que já tem spec começa editando `specs/<slug>/spec.md`** (ou pedindo pro `/architect` desenhar a mudança nela), não direto no código. Ver `specs/README.md`.
- **`git fetch origin` + branch da `develop` atualizada ANTES de escrever qualquer linha.** Ver a regra completa em CI/CD — é o passo zero, não uma checagem opcional.
- Ler controllers/DTOs REAIS antes de assumir shape. Nunca inventar contrato.
- Um chunk por vez: PASSO 0 (contratos/gap) → OK → implementar.
- Rotas: literais antes de :id; públicas fora de guards.
- Labels/textos derivados do estado de domínio (ex: "pagamento" vs "envio" por offerType), não hardcoded.
- **Testar comportamento, não render:** mockar hooks só valida UI. Cobrir o happy path real + os gates de negócio. Antes de construir UI sobre endpoint "que já existe", LER a validação dele (mordeu em Recompensas: create exigia conteúdo aprovado, falhou silencioso em prod).
- **Manter este CLAUDE.md atualizado:** ao concluir feature/release, atualizar Feito / Pendente / Telas prontas.

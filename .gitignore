# ─── Dependencies ──────────────────────────────────────────────────────────────
node_modules/
.pnp
.pnp.js

# ─── Build outputs ─────────────────────────────────────────────────────────────
dist/
build/
.next/
out/

# ─── Environment variables — NEVER commit .env files ──────────────────────────
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
apps/**/.env
apps/**/.env.*
!apps/**/.env.example

# ─── Turbo cache ───────────────────────────────────────────────────────────────
.turbo/

# ─── Testing ───────────────────────────────────────────────────────────────────
coverage/

# ─── Prisma ────────────────────────────────────────────────────────────────────
# Keep schema.prisma and migrations, ignore generated client (it's regenerated)
apps/api/node_modules/.prisma/

# ─── Logs ──────────────────────────────────────────────────────────────────────
*.log
npm-debug.log*

# ─── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ─── IDE ───────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ─── Claude Code — notas locais por dispositivo, não vão pro repo ─────────────
# CLAUDE.md agora é versionado (contexto precisa sobreviver entre dispositivos).
# Detalhe específico de UMA máquina (não do projeto) vai em CLAUDE.local.md.
CLAUDE.local.md

# ─── Misc ──────────────────────────────────────────────────────────────────────
*.tsbuildinfo

.vercel
.env*

# ─── Local tooling ───────────────────────────────────────────────────────────────
# .claude/ é ignorado por padrão, MAS o "cérebro" do produto é versionado de propósito:
# conhecimento (visão/posicionamento/decisões), agentes e comandos precisam sobreviver
# entre máquinas e clones. Mesmo motivo pelo qual o CLAUDE.md foi versionado depois de
# se perder 2x (2026-06-17 e 2026-08-05) enquanto estava aqui.
.claude/*
!.claude/knowledge/
!.claude/agents/
!.claude/commands/

# ─── Design handoffs — material de referência, não vai pro repo ───────────────
apps/web/design_handoff_tayro_2a/
